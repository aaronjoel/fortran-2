program types

  real:: i

  write(*, *) 'Enter a number to divide by 3...'
  read(*, *) i

  write(*, *) i, 'divided by 3 is', i / 3
  ! Exercise: fix this program. First type, then conversion.

end program types
